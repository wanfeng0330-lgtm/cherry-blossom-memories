# Production Environment Variables for Backend

# Database Configuration (Required for production)
USE_MEMORY_DB=false
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cherry-blossom-memories

# Server Configuration (Render will set PORT automatically)
PORT=3001

# JWT Configuration (not used in current version but kept for future)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Node Environment
NODE_ENV=production
