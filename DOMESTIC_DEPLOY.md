# 🇨🇳 国内平台部署指南 - 樱花树时光机

## 📋 推荐方案

| 服务 | 平台 | 费用 | 速度 |
|------|------|------|------|
| 前端 | Gitee Pages | 免费 | 极快 |
| 前端（备选） | Vercel + CDN | 免费 | 较快 |
| 后端 | 腾讯云轻量服务器 | 约50元/年 | 快 |
| 后端（备选） | Railway | 免费 | 较慢 |
| 数据库 | MongoDB Atlas | 免费 | 中等 |

---

## 🚀 方案 A：最经济（完全免费）

### 前端：Gitee Pages（国内访问快）

1. **注册 Gitee**
   - 访问：https://gitee.com/signup
   - 用手机号或邮箱注册

2. **创建仓库**
   - 点击右上角 "+" → "新建仓库"
   - 仓库名：`cherry-blossom-memories`
   - 选择 "私有"（保护隐私）
   - 点击 "创建"

3. **推送代码**
   ```bash
   # 在项目目录下
   cd D:\CC-Switch-v3.10.2-Windows-Portable\cherry-blossom-memories

   git init
   git add .
   git commit -m "Initial commit"

   # 添加 Gitee 远程仓库（替换成你的用户名）
   git remote add gitee https://gitee.com/你的用户名/cherry-blossom-memories.git

   # 推送代码
   git push -u gitee main
   ```

4. **构建前端**
   ```bash
   cd frontend
   npm run build
   ```

5. **开启 Gitee Pages**
   - 进入 Gitee 仓库页面
   - 点击 "服务" → "Gitee Pages"
   - 部署分支：`main`
   - 部署目录：`frontend/dist`（或手动上传）
   - 点击 "启动"
   - 等待几分钟，获得访问地址：`https://你的用户名.gitee.io/cherry-blossom-memories`

### 后端：Railway（免费，从国内访问稍慢）

1. **注册 Railway**
   - 访问：https://railway.app/
   - 用 GitHub 账号登录

2. **部署后端**
   - 点击 "New Project" → "Deploy from GitHub repo"
   - 选择你的仓库
   - 点击 "Add Variables" 添加环境变量：
     ```
     MONGODB_URI=mongodb+srv://用户名:密码@集群.mongodb.net/cherry-blossom-memories
     PORT=3001
     NODE_ENV=production
     ```
   - Railway 会自动检测 Node.js 项目并部署
   - 获得后端 URL：`https://xxx.up.railway.app`

3. **更新前端 API 地址**
   - 修改 `frontend/.env.production`：
     ```
     VITE_API_URL=https://你的后端URL
     ```
   - 重新构建并推送：
     ```bash
     cd frontend
     npm run build
     cd ..
     git add frontend/dist
     git commit -m "Update API URL"
     git push gitee main
     ```

---

## 🏠 方案 B：最佳体验（约50元/年）

### 后端：腾讯云轻量应用服务器

1. **购买服务器**
   - 访问：https://cloud.tencent.com/product/lighthouse
   - 选择：2核2GB（约50元/年）
   - 镜像选择：Node.js 16
   - 地域：选择离您最近的

2. **登录服务器**
   - 获取服务器公网IP
   - 用 SSH 或腾讯云控制台登录

3. **安装 MongoDB**
   ```bash
   # 更新系统
   sudo apt update

   # 安装 MongoDB
   wget -qO - https://www.mongodb.org/static/pgp/server-6.0.asc | sudo apt-key add -
   echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
   sudo apt update
   sudo apt install -y mongodb-org

   # 启动 MongoDB
   sudo systemctl start mongod
   sudo systemctl enable mongod
   ```

4. **上传后端代码**
   ```bash
   # 在本地，将 backend 文件夹打包
   cd D:\CC-Switch-v3.10.2-Windows-Portable\cherry-blossom-memories
   tar -czf backend.tar.gz backend/

   # 通过 SCP 上传（替换成你的服务器IP）
   scp backend.tar.gz root@你的服务器IP:/root/

   # 在服务器上解压
   ssh root@你的服务器IP
   cd /root
   tar -xzf backend.tar.gz
   cd backend
   npm install
   ```

5. **配置环境变量**
   ```bash
   # 创建 .env 文件
   cat > .env << EOF
   MONGODB_URI=mongodb://localhost:27017/cherry-blossom-memories
   PORT=3001
   NODE_ENV=production
   EOF
   ```

6. **使用 PM2 保持运行**
   ```bash
   # 安装 PM2
   npm install -g pm2

   # 启动后端
   pm2 start src/server.js --name cherry-backend

   # 保存 PM2 配置
   pm2 save
   pm2 startup
   ```

7. **配置防火墙**
   - 在腾讯云控制台
   - 安全组 → 添加规则
   - 开放端口：3001

8. **获得后端地址**
   - `http://你的服务器IP:3001`
   - 或配置域名后使用域名

---

## 🎯 方案 C：临时测试（最简单）

### 使用本地服务器 + 内网穿透

如果只是临时让手机访问，不需要部署：

1. **确保手机和电脑在同一 WiFi**

2. **查找电脑 IP**
   ```bash
   ipconfig
   # 找到 "IPv4 地址"，如：192.168.1.100
   ```

3. **启动服务**
   ```bash
   # 后端
   cd backend
   npm start

   # 前端（新终端）
   cd frontend
   npm run dev -- --host
   ```

4. **手机访问**
   - 浏览器输入：`http://192.168.1.100:5173`
   - 输入密码：`zhangyuanxin317`

### 使用内网穿透（外网访问）

如果需要从外网访问，使用内网穿透工具：

**选项 1：cpolar（推荐，简单）**

1. 下载：https://www.cpolar.com/
2. 安装后注册账号
3. 创建隧道：
   - 协议：HTTP
   - 本地地址：127.0.0.1
   - 本地端口：5173（前端）或 3001（后端）
4. 获得公网地址，如：`https://xxx.cpolar.cn`

**选项 2：花生壳**

1. 下载：https://hsk.oray.com/
2. 注册账号
3. 映射本地端口
4. 获得访问地址

---

## 📊 方案对比

| 方案 | 费用 | 速度 | 稳定性 | 难度 |
|------|------|------|--------|------|
| Gitee + Railway | 免费 | 中 | 中 | 简单 |
| Gitee + 腾讯云 | 50元/年 | 快 | 高 | 中等 |
| 本地 + cpolar | 免费 | 中 | 低 | 最简单 |

---

## 🔧 配置说明

### MongoDB Atlas（免费数据库）

如果不想自己安装 MongoDB：

1. 访问：https://www.mongodb.com/cloud/atlas/register
2. 创建免费集群（选择离中国最近的区域）
3. 获取连接字符串
4. 在后端 `.env` 中配置：
   ```
   MONGODB_URI=mongodb+srv://用户名:密码@集群.mongodb.net/cherry-blossom-memories
   ```

---

## ✅ 部署检查清单

### 前端
- [ ] 可以在浏览器打开前端地址
- [ ] 密码页面正常显示
- [ ] 输入密码后能进入主界面
- [ ] API 请求地址正确（浏览器F12查看Network）

### 后端
- [ ] 访问 `/health` 端点返回正常
- [ ] MongoDB 连接成功
- [ ] 可以上传照片
- [ ] 可以上传音频

---

## 🎉 完成！

部署完成后，您就可以：

- 在手机浏览器访问您的网站
- 随时上传照片和音乐
- 与爱人分享美好回忆

---

## 💡 小贴士

1. **首次部署建议先用免费方案**，体验后再考虑付费
2. **Gitee Pages 有更新限制**，频繁更新可能被暂停
3. **腾讯云新用户有优惠**，首年可能更便宜
4. **备份数据**，定期导出 MongoDB 数据

需要帮助？查看部署过程中遇到的具体问题进行排查。
